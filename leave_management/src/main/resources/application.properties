spring.application.name=leave_management

# Server port
server.port=8080

# # CORS Configuration
# spring.web.cors.allowed-origins=http://localhost:8090
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.web.cors.allow-credentials=true

# Kết nối DB SQL Server
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=LeaveManagement;encrypt=false;trustServerCertificate=true
spring.datasource.username=dovuhiep
spring.datasource.password=123456
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# SQL Server Configuration
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true 

